class Book {
  constructor(id, title, author, totalPages, genre, band) {
    this.id = id;
    this.title = title;
    this.author = author;
    this.totalPages = totalPages;
    this.genre = genre;
    this.band = band;
  }
}

export default Book;